{
  "name": "ralph-to-ralph",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run --testTimeout=45000",
    "test:watch": "vitest",
    "validate": "node scripts/validate-schemas.mjs"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "vitest": "^4.1.5"
  }
}
