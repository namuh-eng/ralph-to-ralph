{
  "name": "ralph-to-ralph",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run --testTimeout=45000",
    "test:watch": "vitest",
    "validate": "node scripts/validate-schemas.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "vitest": "^2.1.9"
  }
}
